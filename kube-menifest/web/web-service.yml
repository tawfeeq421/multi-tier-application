apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: prod
  labels:
    app: web
    author: Tawfeeq
spec:
  type: ClusterIP
  ports:
    - targetPort: web-port
      port: 80
      protocol: TCP
  selector:
    app: web-dep
